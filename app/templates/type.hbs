<h2>
  Types
</h2>

<ul>
{{#each type_chips as |type|}}
  {{#link-to 'type.type-id' type.id}}
    {{image-chip
      chipText=type.name
      color=type.name
      chipImage=type.url
    }}
  {{/link-to}}
{{/each}}
</ul>

{{outlet}}
