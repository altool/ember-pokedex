{{evolution-card
  data=model.chain}}

{{#if model.chain.evolves_to}}
  {{#each model.chain.evolves_to as |stage_1|}}
    {{evolution-card
        data=stage_1}}

    {{#if stage_1.evolves_to}}
      {{#each stage_1.evolves_to as |stage_2|}}
        {{evolution-card
          data=stage_2}}
      {{/each}}
    {{/if}}
  {{/each}}
{{/if}}
